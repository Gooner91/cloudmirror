services:
  app:
    build: .
    working_dir: /app
    volumes:
      - ./:/app
      - gobin:/go/bin
      - gomodcache:/go/pkg/mod
    command: ["go", "run", "main.go"]

volumes:
  gomodcache:
  gobin:
